{
  "name": "ldap-connector",
  "version": "0.1.0",
  "description": "LDAP Connector Microservice",
  "main": "index.js",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "build": "yarn tsc",
    "start": "node dist/index.js",
    "watch": "yarn build && node --watch dist/index.js",
    "dev": "yarn build && yarn start",
    "dock": "docker compose down && docker compose build && docker compose up"
  },
  "dependencies": {
    "fastify": "^4.21.0",
    "ldapts": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.8",
    "typescript": "^5.1.6"
  }
}
